본 문서는 현재 진행 중인 작업과 향후 구현할 기능들을 포함합니다.

## 🏃 진행 중 (In Progress)
- [ ] SSE(Server-Sent Events) 상태 동기화 예외 처리 및 최적화
    - 불필요한 이벤트 발행 최소화 및 연결 유실 대비 로직 강화

## 📅 향후 계획 (To Do)
- [ ] 리소스 및 성능 관리 (PDF 처리)
    - OOM 방지를 위한 Stream 기반 PDF 생성 및 다운로드 처리 도입
    - 임시 파일 생명주기(Lifecycle) 관리 로직 강화
- [ ] 코드 품질 및 에러 처리
    - 전역 에러 핸들러 도입 (응답 규격 통일 및 Trace ID 적용)
    - 하드코딩된 매직 넘버/스트링(상태 코드, 타임아웃 등) 환경 변수 분리
- [ ] 신규 기능
    - 다크 모드 지원
    - 사용자 커스텀 폰트 적용 기능

## ✅ 완료됨 (Done)
- [x] 비동기 작업 큐 고도화
    - BullMQ 및 Redis 큐 도입
    - 실패 작업에 대한 재시도(Retry) 및 데드 레터 큐(DLQ) 처리 구현
- [x] 아키텍처 및 책임 분리 (Separation of Concerns)
    - `src/app.js` 내 라우팅, 비즈니스 로직, 스케줄러 계층 분리
    - 큐 설정 및 인프라 커넥션 모듈 분리 (`src/config/queue.js`)
- [x] TDD 기반 회귀 테스트 환경 구축
    - 비즈니스 로직(동시성 제어 등) 단위 테스트 작성
    - 노션 요소별 렌더링 확인 테스트 도입